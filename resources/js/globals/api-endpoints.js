import API_CONFIG from "./api-config";

const API_ENDPOINTS = {
   ART: `${API_CONFIG.BASE_URL}arts`,
   ARTIST: `${API_CONFIG.BASE_URL}artists`,
   EVENT: `${API_CONFIG.BASE_URL}events`,
   MERCH: `${API_CONFIG.BASE_URL}merchs`,
}

export default API_ENDPOINTS;